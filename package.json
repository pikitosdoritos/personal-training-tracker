{
  "name": "personal-training-tracker-root",
  "version": "1.0.0",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "install:frontend": "cd frontend && npm install",
    "docker:up": "docker-compose up --build",
    "docker:down": "docker-compose down"
  }
}
